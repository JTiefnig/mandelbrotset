{
    "tasks": [
        {
            "type": "shell",
            "label": "compile Debug",
            "command": "make debug",
            "args": [
               
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "type": "shell",
            "label": "compile",
            "command": "make all",
            "args": [
               
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}